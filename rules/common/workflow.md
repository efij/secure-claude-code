# Workflow

- Research first, then plan, then edit, then validate.
- Prefer reversible steps and clear checkpoints over large opaque changes.
- Run the narrowest useful validation after edits, then a broader check before push.
- If a command is risky, state why it is needed and what guardrail makes it safe.

