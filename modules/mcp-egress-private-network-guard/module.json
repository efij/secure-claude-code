{
  "id": "mcp-egress-private-network-guard",
  "name": "MCP Egress Private Network Guard Pack",
  "description": "Blocks or prompts on MCP requests that target private IP space, localhost, or link-local destinations according to outbound policy.",
  "category": "mcp",
  "kind": "prompt",
  "default_profiles": ["balanced", "strict"],
  "rules": [
    "security-baseline.md",
    "tool-safety.md"
  ],
  "hook": {
    "event": "PreToolUse",
    "matcher": "mcp__.*",
    "type": "command",
    "command": "bash ~/.runwall/hooks/mcp-egress-private-network-guard.sh \"$TOOL_INPUT\""
  }
}
