{
  "mcpServers": {
    "runwall": {
      "type": "stdio",
      "command": "{{PYTHON_BIN}}",
      "args": [
        "{{ROOT_DIR}}/scripts/runwall_mcp_server.py",
        "--root",
        "{{ROOT_DIR}}",
        "--profile",
        "{{PROFILE}}"
      ],
      "env": {}
    }
  }
}
